<h2>Galerie</h2>
<div id="grid-galerie">
<%
    var counter = 0;
    var d=new Date();
    var ora_crt = d.getHours();

    var timp = ["dimineata", "pranz", "seara"];
   
    for (let image of locals.imagini) {
        if ((5 <= ora_crt < 12 && image.timp.includes("dimieata")) ||
            (12 <= ora_crt < 20 && image.timp.includes("pranz")) ||
            (20 <= ora_crt < 5 && image.timp.includes("seara")))
        {
            counter++;
%>
    <figure>
        <picture>
            <source srcset="<%- image.fisier_mic %>" media="(max-width: 700px)">
            <source srcset="<%- image.fisier_mediu %>" media="(min-width: 700px) and (max-width: 1200px)">
            <img src="<%- image.fisier_mare %>" title="<%- image.titlu %>" alt="<%- image.titlu %>">
        </picture>
        <figcaption>
            <%- image.descriere %>
            <br>
            <% if (image.autor) { %>
                <em>Autor: <%- image.autor %></em>
            <% } %>
        </figcaption>
    </figure>
<% 
        }      
        if(counter == 6) {
            break;
        }
    }
%>
</div>